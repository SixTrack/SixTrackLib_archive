#include<stdio.h>

#define make_fun(name,i) \
    int name##(int a){ \
         return a+i;    \
        };

make_fun(fun1,2);

make_fun(fun2,3);

int main()
{
int a=5;
printf("%d\n",fun1(a));
printf("%d\n",fun2(a));
}
